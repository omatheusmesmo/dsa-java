package dev.omatheus.dsa.trees.medium;

import dev.omatheus.dsa.trees.TreeNode;

/**
 * <a href="https://leetcode.com/problems/validate-binary-search-tree/">98. Validate Binary Search Tree</a> (Medium)
 *
 * <p>Given the root of a binary tree, determine if it is a valid binary search tree (BST). A valid
 * BST is defined as follows: The left subtree of a node contains only nodes with keys less than
 * the node's key. The right subtree of a node contains only nodes with keys greater than the
 * node's key. Both the left and right subtrees must also be binary search trees.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: root = [2,1,3]
 * Output: true
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>The number of nodes in the tree is in the range [1, 10^4]</li>
 *   <li>-2^31 <= Node.val <= 2^31 - 1</li>
 * </ul>
 *
 * @see ValidateBinarySearchTreeTest
 */
public class ValidateBinarySearchTree {

    /**
     * Checks if the binary tree is a valid BST.
     *
     * @param root the root of the binary tree
     * @return true if valid BST, false otherwise
     */
    public boolean isValidBST(TreeNode root) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
