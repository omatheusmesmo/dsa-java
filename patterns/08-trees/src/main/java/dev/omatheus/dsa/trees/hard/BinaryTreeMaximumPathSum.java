package dev.omatheus.dsa.trees.hard;

import dev.omatheus.dsa.trees.TreeNode;

/**
 * <a href="https://leetcode.com/problems/binary-tree-maximum-path-sum/">124. Binary Tree Maximum Path Sum</a> (Hard)
 *
 * <p>A path in a binary tree is a sequence of nodes where each pair of adjacent nodes in the
 * sequence has an edge connecting them. A node can only appear in the sequence at most once.
 * The path does not need to pass through the root. The path sum of a path is the sum of the
 * node's values in the path. Given the root of a binary tree, return the maximum path sum of
 * any non-empty path.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: root = [1,2,3]
 * Output: 6
 * Explanation: The optimal path is 2 -> 1 -> 3 with a path sum of 2 + 1 + 3 = 6.
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>The number of nodes in the tree is in the range [1, 3 * 10^4]</li>
 *   <li>-1000 <= Node.val <= 1000</li>
 * </ul>
 *
 * @see BinaryTreeMaximumPathSumTest
 */
public class BinaryTreeMaximumPathSum {

    /**
     * Finds the maximum path sum in the binary tree.
     *
     * @param root the root of the binary tree
     * @return the maximum path sum
     */
    public int maxPathSum(TreeNode root) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
