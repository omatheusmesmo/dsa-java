package dev.omatheus.dsa.trees.easy;

import dev.omatheus.dsa.trees.TreeNode;

/**
 * <a href="https://leetcode.com/problems/diameter-of-binary-tree/">543. Diameter of Binary Tree</a> (Easy)
 *
 * <p>Given the root of a binary tree, return the length of the diameter of the tree. The diameter
 * of a binary tree is the length of the longest path between any two nodes in a tree. This path
 * may or may not pass through the root. The length of a path between two nodes is represented
 * by the number of edges between them.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: root = [1,2,3,4,5]
 * Output: 3
 * Explanation: 3 is the length of the path [4,2,1,3] or [5,2,1,3].
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>The number of nodes in the tree is in the range [1, 10^4]</li>
 *   <li>-100 <= Node.val <= 100</li>
 * </ul>
 *
 * @see DiameterOfBinaryTreeTest
 */
public class DiameterOfBinaryTree {

    /**
     * Calculates the diameter of the binary tree.
     *
     * @param root the root of the binary tree
     * @return the length of the diameter
     */
    public int diameterOfBinaryTree(TreeNode root) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
