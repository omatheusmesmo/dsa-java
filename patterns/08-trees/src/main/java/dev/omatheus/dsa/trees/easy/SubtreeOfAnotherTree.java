package dev.omatheus.dsa.trees.easy;

import dev.omatheus.dsa.trees.TreeNode;

/**
 * <a href="https://leetcode.com/problems/subtree-of-another-tree/">572. Subtree of Another Tree</a> (Easy)
 *
 * <p>Given the roots of two binary trees root and subRoot, return true if there is a subtree of
 * root with the same structure and node values of subRoot. A subtree of a binary tree tree is a
 * tree that consists of a node in tree and all of this node's descendants.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: root = [3,4,5,1,2], subRoot = [4,1,2]
 * Output: true
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>The number of nodes in the root tree is in the range [1, 2000]</li>
 *   <li>The number of nodes in the subRoot tree is in the range [1, 1000]</li>
 * </ul>
 *
 * @see SubtreeOfAnotherTreeTest
 */
public class SubtreeOfAnotherTree {

    /**
     * Checks if subRoot is a subtree of root.
     *
     * @param root the root of the main tree
     * @param subRoot the root of the potential subtree
     * @return true if subRoot is a subtree of root
     */
    public boolean isSubtree(TreeNode root, TreeNode subRoot) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
