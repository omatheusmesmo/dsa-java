package dev.omatheus.dsa.trees.easy;

import dev.omatheus.dsa.trees.TreeNode;

/**
 * <a href="https://leetcode.com/problems/invert-binary-tree/">226. Invert Binary Tree</a> (Easy)
 *
 * <p>Given the root of a binary tree, invert the tree, and return its root. Inverting a binary
 * tree means swapping the left and right children of every node.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: root = [4,2,7,1,3,6,9]
 * Output: [4,7,2,9,6,3,1]
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>The number of nodes in the tree is in the range [0, 100]</li>
 *   <li>-100 <= Node.val <= 100</li>
 * </ul>
 *
 * @see InvertBinaryTreeTest
 */
public class InvertBinaryTree {

    /**
     * Inverts a binary tree by swapping left and right children of every node.
     *
     * @param root the root of the binary tree
     * @return the root of the inverted binary tree
     */
    public TreeNode invertTree(TreeNode root) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
