package dev.omatheus.dsa.trees.medium;

import dev.omatheus.dsa.trees.TreeNode;

/**
 * <a href="https://leetcode.com/problems/kth-smallest-element-in-a-bst/">230. Kth Smallest Element in a BST</a> (Medium)
 *
 * <p>Given the root of a binary search tree, and an integer k, return the kth smallest value
 * (1-indexed) of all the values of the nodes in the tree.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: root = [3,1,4,null,2], k = 1
 * Output: 1
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>The number of nodes in the tree is n</li>
 *   <li>1 <= k <= n <= 10^4</li>
 *   <li>0 <= Node.val <= 10^4</li>
 * </ul>
 *
 * @see KthSmallestElementInBSTTest
 */
public class KthSmallestElementInBST {

    /**
     * Finds the kth smallest element in a BST.
     *
     * @param root the root of the BST
     * @param k the kth smallest to find (1-indexed)
     * @return the kth smallest value
     */
    public int kthSmallest(TreeNode root, int k) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
