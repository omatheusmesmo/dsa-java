package dev.omatheus.dsa.trees.easy;

import dev.omatheus.dsa.trees.TreeNode;

/**
 * <a href="https://leetcode.com/problems/same-tree/">100. Same Tree</a> (Easy)
 *
 * <p>Given the roots of two binary trees p and q, write a function to check if they are the same
 * or not. Two binary trees are considered the same if they are structurally identical and the
 * nodes have the same value.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: p = [1,2,3], q = [1,2,3]
 * Output: true
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>The number of nodes in both trees is in the range [0, 100]</li>
 *   <li>-100 <= Node.val <= 100</li>
 * </ul>
 *
 * @see SameTreeTest
 */
public class SameTree {

    /**
     * Checks if two binary trees are the same.
     *
     * @param p the root of the first tree
     * @param q the root of the second tree
     * @return true if the trees are the same, false otherwise
     */
    public boolean isSameTree(TreeNode p, TreeNode q) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
