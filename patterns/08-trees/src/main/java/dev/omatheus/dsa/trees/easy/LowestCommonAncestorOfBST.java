package dev.omatheus.dsa.trees.easy;

import dev.omatheus.dsa.trees.TreeNode;

/**
 * <a href="https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-search-tree/">235. Lowest Common Ancestor of a Binary Search Tree</a> (Easy)
 *
 * <p>Given a binary search tree (BST), find the lowest common ancestor (LCA) of two given nodes
 * in the BST. According to the definition of LCA on Wikipedia: The lowest common ancestor is
 * defined between two nodes p and q as the lowest node that has both p and q as descendants
 * (where we allow a node to be a descendant of itself).</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: root = [6,2,8,0,4,7,9,null,null,3,5], p = 2, q = 8
 * Output: 6
 * Explanation: The LCA of nodes 2 and 8 is 6.
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>The number of nodes in the tree is in the range [2, 10^5]</li>
 *   <li>-10^9 <= Node.val <= 10^9</li>
 *   <li>All Node.val are unique</li>
 *   <li>p != q</li>
 *   <li>p and q will exist in the BST</li>
 * </ul>
 *
 * @see LowestCommonAncestorOfBSTTest
 */
public class LowestCommonAncestorOfBST {

    /**
     * Finds the lowest common ancestor of two nodes in a BST.
     *
     * @param root the root of the BST
     * @param p the first node
     * @param q the second node
     * @return the lowest common ancestor
     */
    public TreeNode lowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
