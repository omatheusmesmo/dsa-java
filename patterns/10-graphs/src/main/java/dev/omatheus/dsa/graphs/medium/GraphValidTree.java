package dev.omatheus.dsa.graphs.medium;

/**
 * <a href="https://leetcode.com/problems/graph-valid-tree/">261. Graph Valid Tree</a> (Medium)
 *
 * <p>You have n labeled nodes numbered from 0 to n - 1. You are given an integer n and an array
 * edges where edges[i] = [ai, bi] indicates that there is an undirected edge between nodes ai
 * and bi. Return true if the given edges make up a valid tree, and false otherwise.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: n = 5, edges = [[0,1],[0,2],[0,3],[1,4]]
 * Output: true
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= n <= 2000</li>
 *   <li>0 <= edges.length <= 5000</li>
 * </ul>
 *
 * @see GraphValidTreeTest
 */
public class GraphValidTree {

    /**
     * Determines if the edges form a valid tree.
     *
     * @param n number of nodes
     * @param edges array of [node1, node2] edges
     * @return true if valid tree
     */
    public boolean validTree(int n, int[][] edges) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
