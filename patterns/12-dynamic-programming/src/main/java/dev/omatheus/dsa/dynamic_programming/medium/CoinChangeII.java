package dev.omatheus.dsa.dynamic_programming.medium;

/**
 * <a href="https://leetcode.com/problems/coin-change-ii/">518. Coin Change II</a> (Medium)
 *
 * <p>You are given an integer array coins representing coins of different denominations and an
 * integer amount representing a total amount of money. Return the number of combinations that
 * make up that amount. If that amount of money cannot be made up by any combination of the coins,
 * return 0. The answer is guaranteed to fit into a signed 32-bit integer.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: amount = 5, coins = [1,2,5]
 * Output: 4
 * Explanation: 5 ways: 5=5, 5=2+2+1, 5=2+1+1+1, 5=1+1+1+1+1
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= coins.length <= 300</li>
 *   <li>1 <= coins[i] <= 5000</li>
 *   <li>All the values of coins are unique</li>
 *   <li>0 <= amount <= 5000</li>
 * </ul>
 *
 * @see CoinChangeIITest
 */
public class CoinChangeII {

    /**
     * Returns the number of combinations that make up the amount.
     *
     * @param amount the target amount
     * @param coins array of coin denominations
     * @return the number of combinations
     */
    public int change(int amount, int[] coins) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
