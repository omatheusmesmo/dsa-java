package dev.omatheus.dsa.dynamic_programming.medium;

/**
 * <a href="https://leetcode.com/problems/unique-paths/">62. Unique Paths</a> (Medium)
 *
 * <p>There is a robot on an m x n grid. The robot is initially located at the top-left corner
 * (i.e., grid[0][0]). The robot tries to move to the bottom-right corner (i.e., grid[m-1][n-1]).
 * The robot can only move either down or right at any point in time. Given the two integers m and n,
 * return the number of possible unique paths that the robot can take to reach the bottom-right corner.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: m = 3, n = 7
 * Output: 28
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= m, n <= 100</li>
 * </ul>
 *
 * @see UniquePathsTest
 */
public class UniquePaths {

    /**
     * Returns the number of unique paths in the grid.
     *
     * @param m number of rows
     * @param n number of columns
     * @return the number of unique paths
     */
    public int uniquePaths(int m, int n) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
