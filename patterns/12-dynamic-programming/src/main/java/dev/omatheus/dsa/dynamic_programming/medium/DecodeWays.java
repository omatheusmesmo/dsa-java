package dev.omatheus.dsa.dynamic_programming.medium;

/**
 * <a href="https://leetcode.com/problems/decode-ways/">91. Decode Ways</a> (Medium)
 *
 * <p>A message containing letters from A-Z can be encoded into numbers using the following mapping:
 * 'A' -> "1", 'B' -> "2", ... 'Z' -> "26". Given a string s of digits, return the number of
 * ways to decode it.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: s = "226"
 * Output: 3
 * Explanation: "226" could be decoded as "BZ" (2 26), "VF" (22 6), or "BBF" (2 2 6).
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= s.length <= 100</li>
 *   <li>s consists of only digits and may contain leading zeros</li>
 * </ul>
 *
 * @see DecodeWaysTest
 */
public class DecodeWays {

    /**
     * Returns the number of ways to decode the string.
     *
     * @param s the digit string
     * @return the number of decoding ways
     */
    public int numDecodings(String s) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
