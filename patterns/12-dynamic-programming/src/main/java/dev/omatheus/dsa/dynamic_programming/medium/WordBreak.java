package dev.omatheus.dsa.dynamic_programming.medium;

import java.util.List;

/**
 * <a href="https://leetcode.com/problems/word-break/">139. Word Break</a> (Medium)
 *
 * <p>Given a string s and a dictionary of strings wordDict, return true if s can be segmented
 * into a space-separated sequence of one or more dictionary words. Note that the same word in
 * the dictionary may be reused multiple times in the segmentation.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: s = "leetcode", wordDict = ["leet","code"]
 * Output: true
 * Explanation: Return true because "leetcode" can be segmented as "leet code".
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= s.length <= 300</li>
 *   <li>1 <= wordDict.length <= 1000</li>
 *   <li>1 <= wordDict[i].length <= 20</li>
 * </ul>
 *
 * @see WordBreakTest
 */
public class WordBreak {

    /**
     * Checks if the string can be segmented into dictionary words.
     *
     * @param s the input string
     * @param wordDict the dictionary of words
     * @return true if segmentation is possible
     */
    public boolean wordBreak(String s, List<String> wordDict) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
