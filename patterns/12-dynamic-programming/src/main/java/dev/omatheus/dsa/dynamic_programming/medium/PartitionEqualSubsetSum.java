package dev.omatheus.dsa.dynamic_programming.medium;

/**
 * <a href="https://leetcode.com/problems/partition-equal-subset-sum/">416. Partition Equal Subset Sum</a> (Medium)
 *
 * <p>Given a non-empty array nums of positive integers, determine if the array can be partitioned
 * into two subsets such that the sum of elements in both subsets is equal.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: nums = [1,5,11,5]
 * Output: true
 * Explanation: The array can be partitioned as [1, 5, 5] and [11].
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= nums.length <= 200</li>
 *   <li>1 <= nums[i] <= 100</li>
 * </ul>
 *
 * @see PartitionEqualSubsetSumTest
 */
public class PartitionEqualSubsetSum {

    /**
     * Checks if the array can be partitioned into two equal sum subsets.
     *
     * @param nums the input array
     * @return true if partition is possible
     */
    public boolean canPartition(int[] nums) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
