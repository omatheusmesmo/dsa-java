package dev.omatheus.dsa.dynamic_programming.easy;

/**
 * <a href="https://leetcode.com/problems/min-cost-climbing-stairs/">746. Min Cost Climbing Stairs</a> (Easy)
 *
 * <p>You are given an integer array cost where cost[i] is the cost of ith step on a staircase.
 * Once you pay the cost, you can either climb one or two steps. You can either start from the
 * step with index 0, or the step with index 1. Return the minimum cost to reach the top of the
 * floor.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: cost = [10,15,20]
 * Output: 15
 * Explanation: You will start at index 1, pay 15 and climb two steps to reach the top.
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>2 <= cost.length <= 1000</li>
 *   <li>0 <= cost[i] <= 999</li>
 * </ul>
 *
 * @see MinCostClimbingStairsTest
 */
public class MinCostClimbingStairs {

    /**
     * Returns the minimum cost to reach the top.
     *
     * @param cost array of step costs
     * @return the minimum cost
     */
    public int minCostClimbingStairs(int[] cost) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
