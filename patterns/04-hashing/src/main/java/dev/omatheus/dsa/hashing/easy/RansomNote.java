package dev.omatheus.dsa.hashing.easy;

import java.util.HashMap;
import java.util.Map;

/**
 * <a href="https://leetcode.com/problems/ransom-note/">383. Ransom Note</a> (Easy)
 *
 * <p>Given two strings {@code ransomNote} and {@code magazine}, return true if {@code ransomNote}
 * can be constructed by using the letters from {@code magazine} and false otherwise. Each letter
 * in {@code magazine} can only be used once.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: ransomNote = "a", magazine = "b"
 * Output: false
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= ransomNote.length, magazine.length <= 10^5</li>
 *   <li>ransomNote and magazine consist of lowercase English letters</li>
 * </ul>
 *
 * @see dev.omatheus.dsa.hashing.easy.RansomNoteTest
 */
public class RansomNote {

    /**
     * Checks if ransomNote can be constructed from magazine.
     *
     * @param ransomNote the note to construct
     * @param magazine   the available letters
     * @return true if ransomNote can be constructed
     */
    public boolean canConstruct(String ransomNote, String magazine) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
