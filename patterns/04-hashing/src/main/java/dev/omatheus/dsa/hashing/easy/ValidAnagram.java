package dev.omatheus.dsa.hashing.easy;

/**
 * <a href="https://leetcode.com/problems/valid-anagram/">242. Valid Anagram</a> (Easy)
 *
 * <p>Given two strings {@code s} and {@code t}, return true if {@code t} is an anagram of {@code s},
 * and false otherwise.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: s = "anagram", t = "nagaram"
 * Output: true
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= s.length, t.length <= 5 * 10^4</li>
 *   <li>s and t consist of lowercase English letters only</li>
 * </ul>
 *
 * @see dev.omatheus.dsa.hashing.easy.ValidAnagramTest
 */
public class ValidAnagram {

    /**
     * Checks if string t is an anagram of string s.
     *
     * @param s the first string
     * @param t the second string
     * @return true if t is an anagram of s
     */
    public boolean isAnagram(String s, String t) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
