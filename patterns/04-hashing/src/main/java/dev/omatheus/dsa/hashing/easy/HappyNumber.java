package dev.omatheus.dsa.hashing.easy;

import java.util.HashSet;
import java.util.Set;

/**
 * <a href="https://leetcode.com/problems/happy-number/">202. Happy Number</a> (Easy)
 *
 * <p>Write an algorithm to determine if a number {@code n} is happy. A happy number is a number
 * defined by the following process: replace the number by the sum of the squares of its digits,
 * repeat until it equals 1 (where it will stay), or it loops endlessly in a cycle.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: n = 19
 * Output: true
 * Explanation: 1^2 + 9^2 = 82, 8^2 + 2^2 = 68, 6^2 + 8^2 = 100, 1^2 + 0^2 + 0^2 = 1
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= n <= 2^31 - 1</li>
 * </ul>
 *
 * @see dev.omatheus.dsa.hashing.easy.HappyNumberTest
 */
public class HappyNumber {

    /**
     * Determines if n is a happy number.
     *
     * @param n the number to check
     * @return true if n is a happy number
     */
    public boolean isHappy(int n) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
