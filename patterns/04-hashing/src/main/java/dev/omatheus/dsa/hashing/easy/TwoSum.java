package dev.omatheus.dsa.hashing.easy;

/**
 * <a href="https://leetcode.com/problems/two-sum/">1. Two Sum</a> (Easy)
 *
 * <p>Given an array of integers {@code nums} and an integer {@code target}, return indices of the two
 * numbers such that they add up to target. You may assume that each input would have exactly one
 * solution, and you may not use the same element twice.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: nums = [2,7,11,15], target = 9
 * Output: [0,1]
 * Explanation: Because nums[0] + nums[1] == 9, we return [0, 1].
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>2 <= nums.length <= 10^4</li>
 *   <li>-10^9 <= nums[i] <= 10^9</li>
 *   <li>-10^9 <= target <= 10^9</li>
 *   <li>Only one valid answer exists</li>
 * </ul>
 *
 * @see dev.omatheus.dsa.hashing.easy.TwoSumTest
 */
public class TwoSum {

    /**
     * Finds two numbers in the array that add up to the target and returns their indices.
     *
     * @param nums   the input array of integers
     * @param target the target sum
     * @return an array containing the indices of the two numbers
     */
    public int[] twoSum(int[] nums, int target) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
