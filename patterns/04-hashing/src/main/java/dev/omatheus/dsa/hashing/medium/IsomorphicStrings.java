package dev.omatheus.dsa.hashing.medium;

import java.util.HashMap;
import java.util.Map;

/**
 * <a href="https://leetcode.com/problems/isomorphic-strings/">205. Isomorphic Strings</a> (Medium)
 *
 * <p>Given two strings {@code s} and {@code t}, determine if they are isomorphic. Two strings
 * {@code s} and {@code t} are isomorphic if the characters in {@code s} can be replaced to get {@code t}.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: s = "egg", t = "add"
 * Output: true
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= s.length <= 5 * 10^4</li>
 *   <li>1 <= t.length <= 5 * 10^4</li>
 *   <li>s and t consist of any valid ascii character</li>
 * </ul>
 *
 * @see dev.omatheus.dsa.hashing.medium.IsomorphicStringsTest
 */
public class IsomorphicStrings {

    /**
     * Checks if strings s and t are isomorphic.
     *
     * @param s the first string
     * @param t the second string
     * @return true if the strings are isomorphic
     */
    public boolean isIsomorphic(String s, String t) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
