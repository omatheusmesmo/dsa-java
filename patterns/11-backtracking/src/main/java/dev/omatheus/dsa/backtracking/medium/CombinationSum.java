package dev.omatheus.dsa.backtracking.medium;

import java.util.List;

/**
 * <a href="https://leetcode.com/problems/combination-sum/">39. Combination Sum</a> (Medium)
 *
 * <p>Given an array of distinct integers candidates and a target integer target, return a list
 * of all unique combinations of candidates where the chosen numbers sum to target. You may return
 * the combinations in any order. The same number may be chosen from candidates an unlimited number
 * of times.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: candidates = [2,3,6,7], target = 7
 * Output: [[2,2,3],[7]]
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= candidates.length <= 30</li>
 *   <li>2 <= candidates[i] <= 40</li>
 *   <li>All elements of candidates are distinct</li>
 * </ul>
 *
 * @see CombinationSumTest
 */
public class CombinationSum {

    /**
     * Returns all unique combinations that sum to target.
     *
     * @param candidates array of distinct integers
     * @param target the target sum
     * @return list of all unique combinations
     */
    public List<List<Integer>> combinationSum(int[] candidates, int target) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
