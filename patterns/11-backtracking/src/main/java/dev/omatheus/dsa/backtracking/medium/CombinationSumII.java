package dev.omatheus.dsa.backtracking.medium;

import java.util.List;

/**
 * <a href="https://leetcode.com/problems/combination-sum-ii/">40. Combination Sum II</a> (Medium)
 *
 * <p>Given a collection of candidate numbers (candidates) and a target number (target), find all
 * unique combinations in candidates where the candidate numbers sum to target. Each number in
 * candidates may only be used once in the combination. The solution set must not contain duplicate
 * combinations.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: candidates = [10,1,2,7,6,1,5], target = 8
 * Output: [[1,1,6],[1,2,5],[1,7],[2,6]]
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= candidates.length <= 30</li>
 *   <li>1 <= candidates[i] <= 50</li>
 *   <li>All elements of candidates are distinct</li>
 * </ul>
 *
 * @see CombinationSumIITest
 */
public class CombinationSumII {

    /**
     * Returns all unique combinations that sum to target.
     *
     * @param candidates array of integers
     * @param target the target sum
     * @return list of all unique combinations
     */
    public List<List<Integer>> combinationSum2(int[] candidates, int target) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
