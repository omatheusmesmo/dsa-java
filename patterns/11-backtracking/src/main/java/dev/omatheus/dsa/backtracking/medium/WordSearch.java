package dev.omatheus.dsa.backtracking.medium;

/**
 * <a href="https://leetcode.com/problems/word-search/">79. Word Search</a> (Medium)
 *
 * <p>Given an m x n grid of characters board and a string word, return true if word exists in
 * the grid. The word can be constructed from letters of sequentially adjacent cells, where
 * adjacent cells are horizontally or vertically neighboring. The same letter cell may not be
 * used more than once.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: board = [["A","B","C","E"],["S","F","C","S"],["A","D","E","E"]], word = "ABCCED"
 * Output: true
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>m == board.length</li>
 *   <li>n == board[i].length</li>
 *   <li>1 <= m, n <= 6</li>
 *   <li>1 <= word.length <= 15</li>
 * </ul>
 *
 * @see WordSearchTest
 */
public class WordSearch {

    /**
     * Checks if the word exists in the grid.
     *
     * @param board the character grid
     * @param word the word to search for
     * @return true if found, false otherwise
     */
    public boolean exist(char[][] board, String word) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
