package dev.omatheus.dsa.heaps.medium;

/**
 * <a href="https://leetcode.com/problems/kth-largest-element-in-an-array/">215. Kth Largest Element in an Array</a> (Medium)
 *
 * <p>Given an integer array nums and an integer k, return the kth largest element in the array.
 * Note that it is the kth largest element in the sorted order, not the kth distinct element.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: nums = [3,2,1,5,6,4], k = 2
 * Output: 5
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= k <= nums.length <= 10^5</li>
 *   <li>-10^4 <= nums[i] <= 10^4</li>
 * </ul>
 *
 * @see KthLargestElementInAnArrayTest
 */
public class KthLargestElementInAnArray {

    /**
     * Finds the kth largest element in the array.
     *
     * @param nums the array of integers
     * @param k the kth position to find
     * @return the kth largest element
     */
    public int findKthLargest(int[] nums, int k) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
