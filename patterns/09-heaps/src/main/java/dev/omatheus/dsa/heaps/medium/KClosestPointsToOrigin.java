package dev.omatheus.dsa.heaps.medium;

/**
 * <a href="https://leetcode.com/problems/k-closest-points-to-origin/">973. K Closest Points to Origin</a> (Medium)
 *
 * <p>Given an array of points where points[i] = [xi, yi] represents a point on the X-Y plane
 * and an integer k, return the k closest points to the origin (0, 0). The distance between two
 * points on the X-Y plane is the Euclidean distance. You may return the answer in any order.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: points = [[3,3],[5,-1],[-2,4]], k = 2
 * Output: [[3,3],[-2,4]]
 * Explanation: The distance from [3,3] is sqrt(18), from [5,-1] is sqrt(26), from [-2,4] is sqrt(20).
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= k <= points.length <= 10^4</li>
 *   <li>-10^4 <= xi, yi <= 10^4</li>
 * </ul>
 *
 * @see KClosestPointsToOriginTest
 */
public class KClosestPointsToOrigin {

    /**
     * Returns the k closest points to the origin.
     *
     * @param points array of [x, y] points
     * @param k the number of closest points to return
     * @return array of k closest points
     */
    public int[][] kClosest(int[][] points, int k) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
