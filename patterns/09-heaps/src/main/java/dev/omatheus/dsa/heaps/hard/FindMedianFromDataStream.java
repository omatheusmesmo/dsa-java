package dev.omatheus.dsa.heaps.hard;

/**
 * <a href="https://leetcode.com/problems/find-median-from-data-stream/">295. Find Median from Data Stream</a> (Hard)
 *
 * <p>The median is the middle value in an ordered integer list. If the size of the list is even,
 * there is no middle value. So the median is the mean of the two middle values. Implement the
 * MedianFinder class: MedianFinder() initializes the MedianFinder object. void addNum(int num)
 * adds the integer num from the data stream to the data structure. double findMedian() returns
 * the median of all elements so far.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: ["MedianFinder","addNum","addNum","findMedian","addNum","findMedian"]
 * [[],[1],[2],[],[3],[]]
 * Output: [null,null,null,1.5,null,2.0]
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>-10^5 <= num <= 10^5</li>
 *   <li>There will be at least one element in the data structure before calling findMedian</li>
 * </ul>
 *
 * @see FindMedianFromDataStreamTest
 */
public class FindMedianFromDataStream {

    /**
     * Initializes the median finder.
     */
    public FindMedianFromDataStream() {
    }

    /**
     * Adds a number to the data structure.
     *
     * @param num the number to add
     */
    public void addNum(int num) {
        throw new UnsupportedOperationException("Solve this problem!");
    }

    /**
     * Returns the median of all elements so far.
     *
     * @return the median value
     */
    public double findMedian() {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
