package dev.omatheus.dsa.slidingwindow.medium;

/**
 * <a href="https://leetcode.com/problems/longest-repeating-character-replacement/">424. Longest Repeating Character Replacement</a> (Medium)
 *
 * <p>You are given a string {@code s} and an integer {@code k}. You can replace at most {@code k}
 * characters to make all characters the same. Return the length of the longest substring with
 * all the same characters.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: s = "AABABBA", k = 1
 * Output: 4
 * Explanation: Replace the one 'A' in the middle with 'B' and form "AABBBBA".
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= s.length <= 10^5</li>
 *   <li>s consists of uppercase English letters only</li>
 *   <li>0 <= k <= s.length</li>
 * </ul>
 *
 * @see dev.omatheus.dsa.slidingwindow.medium.LongestRepeatingCharacterReplacementTest
 */
public class LongestRepeatingCharacterReplacement {

    /**
     * Finds the length of the longest substring where all characters are the same
     * after at most k replacements.
     *
     * @param s the input string
     * @param k the maximum number of replacements allowed
     * @return the length of the longest substring
     */
    public int characterReplacement(String s, int k) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
