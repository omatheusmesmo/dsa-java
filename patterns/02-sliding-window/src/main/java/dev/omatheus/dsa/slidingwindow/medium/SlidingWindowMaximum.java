package dev.omatheus.dsa.slidingwindow.medium;

import java.util.ArrayDeque;
import java.util.Deque;

/**
 * <a href="https://leetcode.com/problems/sliding-window-maximum/">239. Sliding Window Maximum</a> (Hard)
 *
 * <p>You are given an array of integers {@code nums}, there is a sliding window of size {@code k}
 * which is moving from the very left of the array to the very right. Return the max value in each window.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: nums = [1,3,-1,-3,5,3,6,7], k = 3
 * Output: [3,3,5,5,6,7]
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= nums.length <= 10^5</li>
 *   <li>-10^4 <= nums[i] <= 10^4</li>
 *   <li>1 <= k <= nums.length</li>
 * </ul>
 *
 * @see dev.omatheus.dsa.slidingwindow.medium.SlidingWindowMaximumTest
 */
public class SlidingWindowMaximum {

    /**
     * Returns the maximum value in each sliding window of size k.
     *
     * @param nums the input array
     * @param k    the window size
     * @return an array of maximum values for each window
     */
    public int[] maxSlidingWindow(int[] nums, int k) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
