package dev.omatheus.dsa.slidingwindow.medium;

/**
 * <a href="https://leetcode.com/problems/minimum-size-subarray-sum/">209. Minimum Size Subarray Sum</a> (Medium)
 *
 * <p>Given an array of positive integers {@code nums} and a positive integer {@code target},
 * return the minimal length of a subarray whose sum is greater than or equal to {@code target}.
 * If there is no such subarray, return 0 instead.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: target = 7, nums = [2,3,1,2,4,3]
 * Output: 2
 * Explanation: The subarray [4,3] has the minimal length under the problem constraint.
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= target <= 10^9</li>
 *   <li>1 <= nums.length <= 10^5</li>
 *   <li>1 <= nums[i] <= 10^4</li>
 * </ul>
 *
 * @see dev.omatheus.dsa.slidingwindow.medium.MinimumSizeSubarraySumTest
 */
public class MinimumSizeSubarraySum {

    /**
     * Finds the minimal length of a subarray with sum >= target.
     *
     * @param target the target sum
     * @param nums   the input array
     * @return the minimal length, or 0 if no such subarray exists
     */
    public int minSubArrayLen(int target, int[] nums) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
