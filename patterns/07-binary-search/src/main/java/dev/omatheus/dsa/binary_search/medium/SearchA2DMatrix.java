package dev.omatheus.dsa.binary_search.medium;

/**
 * <a href="https://leetcode.com/problems/search-a-2d-matrix/">74. Search a 2D Matrix</a> (Medium)
 *
 * <p>You are given an m x n integer matrix matrix with the following two properties:
 * Each row is sorted in non-decreasing order. The first integer of each row is greater than the
 * last integer of the previous row. Given an integer target, return true if target is in matrix
 * or false otherwise.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: matrix = [[1,3,5,7],[10,11,16,20],[23,30,34,60]], target = 3
 * Output: true
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>m == matrix.length</li>
 *   <li>n == matrix[i].length</li>
 *   <li>1 <= m, n <= 100</li>
 *   <li>-10^4 <= matrix[i][j], target <= 10^4</li>
 * </ul>
 *
 * @see SearchA2DMatrixTest
 */
public class SearchA2DMatrix {

    /**
     * Searches for a value in the 2D matrix.
     *
     * @param matrix the 2D matrix to search
     * @param target the value to search for
     * @return true if found, false otherwise
     */
    public boolean searchMatrix(int[][] matrix, int target) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
