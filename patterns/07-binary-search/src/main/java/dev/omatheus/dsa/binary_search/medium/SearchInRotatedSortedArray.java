package dev.omatheus.dsa.binary_search.medium;

/**
 * <a href="https://leetcode.com/problems/search-in-rotated-sorted-array/">33. Search in Rotated Sorted Array</a> (Medium)
 *
 * <p>There is an integer array nums sorted in ascending order (with distinct values). Prior to
 * being passed to your function, nums is possibly rotated at an unknown pivot index k (1 <= k < nums.length)
 * such that the resulting array is [nums[k], nums[k+1], ..., nums[n-1], nums[0], nums[1], ..., nums[k-1]].
 * Given the array nums after the possible rotation and an integer target, return the index of target
 * if it is in nums, or -1 if it is not in nums.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: nums = [4,5,6,7,0,1,2], target = 0
 * Output: 4
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= nums.length <= 5000</li>
 *   <li>-10^4 <= nums[i] <= 10^4</li>
 *   <li>All values of nums are unique</li>
 *   <li>nums is an ascending array that is possibly rotated</li>
 * </ul>
 *
 * @see SearchInRotatedSortedArrayTest
 */
public class SearchInRotatedSortedArray {

    /**
     * Searches for a target in a rotated sorted array.
     *
     * @param nums the rotated sorted array
     * @param target the value to search for
     * @return the index of target, or -1 if not found
     */
    public int search(int[] nums, int target) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
