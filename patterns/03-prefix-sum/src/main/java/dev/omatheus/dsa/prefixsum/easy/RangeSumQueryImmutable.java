package dev.omatheus.dsa.prefixsum.easy;

/**
 * <a href="https://leetcode.com/problems/range-sum-query-immutable/">303. Range Sum Query - Immutable</a> (Easy)
 *
 * <p>Given an integer array {@code nums}, handle multiple queries of the type: calculate the sum
 * of the elements of {@code nums} between indices {@code left} and {@code right} inclusive.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: nums = [-2, 0, 3, -5, 2, -1]
 * sumRange(0, 2) -> 1
 * sumRange(2, 5) -> -1
 * sumRange(0, 5) -> -3
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= nums.length <= 10^4</li>
 *   <li>-10^5 <= nums[i] <= 10^5</li>
 *   <li>0 <= left <= right < nums.length</li>
 *   <li>At most 10^4 calls to sumRange</li>
 * </ul>
 *
 * @see dev.omatheus.dsa.prefixsum.easy.RangeSumQueryImmutableTest
 */
public class RangeSumQueryImmutable {

    private final int[] prefixSum;

    /**
     * Initializes the object with the integer array.
     *
     * @param nums the input array
     */
    public RangeSumQueryImmutable(int[] nums) {
        throw new UnsupportedOperationException("Solve this problem!");
    }

    /**
     * Calculates the sum of elements between indices left and right (inclusive).
     *
     * @param left  the left index
     * @param right the right index
     * @return the sum of elements in the range
     */
    public int sumRange(int left, int right) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
