package dev.omatheus.dsa.prefixsum.medium;

/**
 * <a href="https://leetcode.com/problems/product-of-array-except-self/">238. Product of Array Except Self</a> (Medium)
 *
 * <p>Given an integer array {@code nums}, return an array {@code answer} such that {@code answer[i]}
 * is equal to the product of all the elements of {@code nums} except {@code nums[i]}. You must
 * write an algorithm that runs in O(n) time and without using the division operation.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: nums = [1,2,3,4]
 * Output: [24,12,8,6]
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>2 <= nums.length <= 10^5</li>
 *   <li>-30 <= nums[i] <= 30</li>
 *   <li>product of any prefix or suffix fits in 32-bit integer</li>
 * </ul>
 *
 * @see dev.omatheus.dsa.prefixsum.medium.ProductOfArrayExceptSelfTest
 */
public class ProductOfArrayExceptSelf {

    /**
     * Returns an array where each element is the product of all other elements.
     *
     * @param nums the input array
     * @return the product array
     */
    public int[] productExceptSelf(int[] nums) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
