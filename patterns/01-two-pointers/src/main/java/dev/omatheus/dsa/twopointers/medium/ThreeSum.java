package dev.omatheus.dsa.twopointers.medium;

import java.util.List;

/**
 * <a href="https://leetcode.com/problems/3sum/">15. 3Sum</a> (Medium)
 *
 * <p>Given an integer array {@code nums}, return all the triplets {@code [nums[i], nums[j], nums[k]]}
 * such that {@code i != j}, {@code i != k}, and {@code j != k}, and
 * {@code nums[i] + nums[j] + nums[k] == 0}.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: nums = [-1,0,1,2,-1,-4]
 * Output: [[-1,-1,2],[-1,0,1]]
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>3 <= nums.length <= 3000</li>
 *   <li>-10^5 <= nums[i] <= 10^5</li>
 * </ul>
 *
 * @see dev.omatheus.dsa.twopointers.medium.ThreeSumTest
 */
public class ThreeSum {

    /**
     * Finds all unique triplets in the array that sum to zero.
     *
     * @param nums the input array
     * @return a list of unique triplets that sum to zero
     */
    public List<List<Integer>> threeSum(int[] nums) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
