package dev.omatheus.dsa.twopointers.medium;

/**
 * <a href="https://leetcode.com/problems/sort-colors/">75. Sort Colors</a> (Medium)
 *
 * <p>Given an array {@code nums} with {@code n} objects colored red, white, or blue, sort them
 * in-place so that objects of the same color are adjacent, with the colors in the order
 * red, white, and blue (0, 1, 2). This is the Dutch National Flag problem.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: nums = [2,0,2,1,1,0]
 * Output: [0,0,1,1,2,2]
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>n == nums.length</li>
 *   <li>1 <= n <= 300</li>
 *   <li>nums[i] is either 0, 1, or 2</li>
 * </ul>
 *
 * @see dev.omatheus.dsa.twopointers.medium.SortColorsTest
 */
public class SortColors {

    /**
     * Sorts the array in-place so that 0s come first, then 1s, then 2s.
     *
     * @param nums the array to sort
     */
    public void sortColors(int[] nums) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
