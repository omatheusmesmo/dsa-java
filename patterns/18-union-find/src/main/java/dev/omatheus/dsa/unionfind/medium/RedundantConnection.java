package dev.omatheus.dsa.unionfind.medium;

/**
 * <a href="https://leetcode.com/problems/redundant-connection/">684. Redundant Connection</a> (Medium)
 *
 * <p>Find the edge that can be removed to still have a valid tree (cycle detection).</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: edges = [[1,2],[1,3],[2,3]]
 * Output: [2,3]
 * Explanation: Removing [2,3] leaves a valid tree.
 * </pre>
 *
 * <p><strong>Constraints:</strong></p>
 * <ul>
 *   <li>1 <= n <= 1000</li>
 *   <li>1 <= edges.length <= 1000</li>
 *   <li>edges[i].length == 2</li>
 * </ul>
 *
 * @see RedundantConnectionTest
 */
public class RedundantConnection {

    /**
     * Finds the redundant connection that creates a cycle.
     *
     * @param edges the edge list
     * @return the redundant edge
     */
    public int[] findRedundantConnection(int[][] edges) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
