package dev.omatheus.dsa.bitmanipulation.easy;

/**
 * <a href="https://leetcode.com/problems/single-number/">136. Single Number</a> (Easy)
 *
 * <p>Given a non-empty array of integers, every element appears twice except for one.
 * Find that single one. Must run in O(n) time and O(1) space.</p>
 *
 * <p><strong>Example:</strong></p>
 * <pre>
 * Input: nums = [2,2,1]
 * Output: 1
 * </pre>
 *
 * <p><strong>Constraints:</strong></p *   <ul>
 *   <li>1 <= nums.length <= 3 * 10^4</li>
 *   <li>Each element appears once or twice</li>
 * </ul>
 *
 * @see SingleNumberTest
 */
public class SingleNumber {

    /**
     * Finds the single number that does not appear twice.
     *
     * @param nums the input array
     * @return the single number
     */
    public int singleNumber(int[] nums) {
        throw new UnsupportedOperationException("Solve this problem!");
    }
}
